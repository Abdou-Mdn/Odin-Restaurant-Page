{
  "name": "5.restaurant-page",
  "version": "1.0.0",
  "description": "restaurant page from the odin project curriculum",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode production",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
